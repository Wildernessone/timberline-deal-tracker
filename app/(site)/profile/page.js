// Profile — UI rendered by the (site) shell (view derived from the /profile URL).
// Personal/auth-gated, so kept out of the index.
export const metadata = {
  title: "Your profile",
  robots: { index: false, follow: false },
};

export default function ProfilePage() {
  return null;
}
